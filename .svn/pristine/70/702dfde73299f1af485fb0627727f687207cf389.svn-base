export interface ICarouselSlide {
    browserId: number;
    imagePath: string;
    duration: number;
    isActive: boolean;
    createdDate: string; // Use `Date` if parsing it into a Date object
    endDate: string;     // Use `Date` if parsing it into a Date object
    tenantID: number;
  }
  